# Omnigate

omnigate 是浏览器能力库：操控真实 Edge、复用登录态、提取音视频。本上下文覆盖浏览器操控之外的三块：反爬、验证码兜底、任务执行模式。

## 反爬 (Anti-Bot)

**反爬层**:
一组让浏览器"看起来像真人"的技术，目的是减少被目标站点拦截/触发验证码。本项目的反爬手段：真实 Edge 登录态 + 行为特征（而非伪造指纹）。**决策（2026-08-08）**：本轮反爬层 = 完善真实登录态链路 + "无头被拒自动转有头"，不注入反检测脚本（裸 CDP 无 webdriver 特征，注入反可能画蛇添足）。
_Avoid_: 伪装, 隐身, stealth

**反检测**:
针对浏览器自动化特征（如 `navigator.webdriver`）的抹除手段。属于反爬层的一部分，本地可测。
_Avoid_: 反爬（反爬是更上层、更大的概念）

**指纹**:
浏览器暴露给网站的识别特征（Canvas/WebGL/UA/插件等）。本项目不伪造指纹，靠真实 Edge 登录态。
_Avoid_: fingerprint, 伪装

## 验证码 (CAPTCHA)

**验证码**:
目标站点用来区分真人/机器人的交互挑战（图形选字、滑块、reCAPTCHA/Turnstile 等）。
_Avoid_: 验证, 人机验证（太泛）

**兜底**:
当自动化无法通过验证码时，把浏览器交给人类解决的机制。本项目 = 同机弹窗（headless 转 headed）。**决策（2026-08-08）**：
- 验证码检测以页面 DOM 特征为主（recaptcha/turnstile/hcaptcha iframe 等），行为卡顿仅辅助，避免误判。
- 状态迁移：记录 URL → 关无头 → 重启有头 → 重新注入 cookie → 导航回 → 人解 → 继续。物理持久 profile 已验不可靠，不用。
- **非阻塞 + 模型决策**：检测到验证码不自动弹窗，命令继续返回页面内容并输出 `⚠ CAPTCHA` 事件。由大模型判断：误报则忽略继续，确认受阻则调 `--solve-captcha` 弹窗。skill 提前写死全部状态规则，模型不瞎猜。
- 轮询：每几秒查验证码特征，**消失即继续；3 分钟超时**报 `CAPTCHA_TIMEOUT`。
- **CLI 即协议**：stdout 输出就是模型可见的状态。无"人敲/模型敲"分流。
_Avoid_: 降级, fallback（保持中文主词）

**自动解**:
用程序自动通过验证码（识别字符/拖动滑块）。本项目明确**不做**——复杂度过高，且本地无可靠开源方案。遇到验证码一律走兜底（人类解）。
_Avoid_: 无（唯一确定性决策）

## 任务执行模式 (Execution Modes)

**剧本 (Script)**:
固定步骤序列（如 navigate→scroll→click→extract），可回放、可复用。相当于"背熟的流程"。
_Avoid_: 脚本（跟代码脚本混淆）, recipe

**即兴 (Improv)**:
由 LLM 每步决策的自主执行，用于没跑过/会变的任务。首次跑通后固化成剧本。
_Avoid_: 自由模式, agent mode（保持中文）

**剧本固化 (Script-ification)**:
把一次成功的即兴执行记录成剧本，下次直接走剧本不耗 LLM。**决策（2026-08-08）**：剧本 = A 型（记录 CDP 命令序列 JSON），由大模型维护。不做 B 型语义目标（依赖 LLM，且不存经验，脱裤子放屁）。
_Avoid_: 学习, 记忆（这两个词已有含义）

**教训文件 (Lessons)**:
大模型试错后写下的网站经验，存于 `lessons/<网站>.md`（如 bilibili.md）。下次碰同网站先读，避免重复试错。**这是积累知识的治本机制**——比语义剧本更直接。由大模型维护，遇到新坑就记录。
_Avoid_: 无

**渐进式披露 (Progressive Disclosure)**:
SKILL.md 只写"有 scripts/ 和 lessons/，用某网站前自己读对应文件"，不把全部内容塞进 skill 本体。模型需要时自行读取。省 context，不重复加载。
_Avoid_: 全量加载

**三层架构 (Three-Layer)**:
脚本命令（omnigate CLI 执行）+ 模型规章（skill 规则）+ 模型探索（教训积累）。
_Avoid_: 无

**失效检测 (Stale Detection)**:
判断已有剧本是否因网页改版而失效（如选择器找不到、页面结构变化）。失效则回退即兴重摸。
_Avoid_: 过期检测

## 准出标准 (Exit Criteria)

**流程测试**:
验证"决策和状态迁移"正确（本地可造场景、可重复、有明确 pass/fail）。本项目准出标准的可靠部分。
_Avoid_: 无

**对抗测试**:
验证"能否真的通过某站点反爬/验证码"。不可控、不可靠，本项目**不承诺**也不作为准出标准。
_Avoid_: 效果测试（误导）
